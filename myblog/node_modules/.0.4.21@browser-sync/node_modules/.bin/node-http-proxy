#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.0.10.4@http-proxy/bin/node-http-proxy" "$@"
  ret=$?
else 
  node  "$basedir/../../../.0.10.4@http-proxy/bin/node-http-proxy" "$@"
  ret=$?
fi
exit $ret
